# build output
dist/
.vercel/
# importer output (contains scraped prospect data — keep it out of git)
out/
# generated types
.astro/

# TinaCMS build/codegen output (paused feature — see the plan this was built
# from; regenerates via `tinacms dev`/`tinacms build`, nothing to preserve)
tina/__generated__/
public/admin/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
